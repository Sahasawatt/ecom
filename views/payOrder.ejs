<%- include('header') %>

<div class="card mt-5">
    <div class="card-body">
        <div class="card-title h5 mt-3">
            บันทึกการรับชําระเงิน บิล : <%= orderId %>
        </div>
        <form method="post" class="mt-3">
            <div>
                <label>วันที่ชําระเงิน</label>
                <input type="date" name="pay_date" class="form-control" id="">
            </div>
            <div class="mt-3">
                <label >หมายเหตุเพิ่มเติม</label>
                <input type="text" name="pay_remark" class="form-control">
            </div>
            <div class="mt-3"></div>
            <button class="btn btn-info" type="submit">
                Save
            </button>
        </form>
    </div>
</div>

<%- include('footer') %>